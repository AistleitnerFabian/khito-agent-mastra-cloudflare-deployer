# Domain docs

Before exploring a domain area, read the root `CONTEXT.md` and relevant `docs/adr/` entries when they exist. If neither exists, proceed normally.

This is a single-context repository:

```
/
├── CONTEXT.md
├── docs/adr/
└── src/
```

Use `CONTEXT.md` vocabulary in issues, proposals, tests, and code. Surface any conflict with an existing ADR rather than silently overriding it.
